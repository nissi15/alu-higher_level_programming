## SQL_introduction

Introduction to SQL with MySQL 8.0: listing, creating, and deleting databases
and tables, inserting and querying rows, and aggregating data with COUNT, AVG,
GROUP BY, ORDER BY, UPDATE, and DELETE.
