## python-network_0

Tasks on HTTP requests with cURL: reading the body and its size, sending GET,
POST, and DELETE requests, passing headers and parameters, and listing the HTTP
methods a server accepts.
