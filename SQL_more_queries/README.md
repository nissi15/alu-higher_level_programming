## SQL_more_queries

More advanced SQL with MySQL 8.0: managing users and privileges, constraints
(NOT NULL, DEFAULT, UNIQUE, PRIMARY KEY, FOREIGN KEY), subqueries, and joining
tables with INNER JOIN and LEFT JOIN across the states/cities and tvshows
databases.
