## python-inheritance

Tasks on Python inheritance: listing object attributes, subclassing built-in
types, checking class relationships, and building a geometry hierarchy
(BaseGeometry, Rectangle, Square) with integer validation and doctest cases.
